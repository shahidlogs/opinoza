URGENT: The duplicate banner is gone, but the real problem remains.

Current status:

* Only ONE Clerk error banner now appears
* Google sign-in still shows "Too many requests. Please try again in a bit."
* Users still cannot sign in or sign up

This means the UI cleanup worked, but the actual auth 429 source was NOT fixed.

Do this exact investigation and fix:

1. FIND THE REAL SOURCE OF 429
   Check whether the 429 is coming from:

* our Express backend/proxy
* any auth middleware
* Clerk proxy routes
* Clerk directly

Add temporary logging for all auth-related requests and responses:

* route
* status code
* whether response is generated by our backend or upstream Clerk
* include headers if helpful (especially rate limit related headers)

2. TEMPORARILY DISABLE ALL CUSTOM AUTH RATE LIMITING

* Remove or bypass ALL auth-specific limiters completely for now
* No burst limiter
* No special limiter on sign-in/sign-up
* No limiter on Google auth routes
* No limiter on Clerk proxy/auth endpoints

3. EXCLUDE AUTH FROM GLOBAL LIMITER
   If a global API/site rate limiter exists, exclude:

* /sign-in
* /sign-up
* /auth/*
* Clerk proxy routes
* any backend endpoint used during Clerk login flow

4. CHECK FOR PROXY LOOPS OR REPEATED BACKEND CALLS

* Verify frontend is not repeatedly loading auth endpoints
* Verify no useEffect or middleware is re-triggering auth session/client/environment requests
* Verify reverse proxy / server rewrite is not causing repeated hits to Clerk endpoints

5. CHECK CLERK CONFIG

* Verify domain settings for opinoza.com are correct
* Verify allowed redirect URLs / callback URLs are correct
* Verify production keys are being used in production
* Verify no misconfigured proxy path is causing Clerk requests to loop or fail

6. NETWORK-TRACE THE GOOGLE SIGN-IN FLOW
   Open browser dev tools / network and inspect the exact request that returns 429.
   Report back:

* request URL
* who returned it
* response body
* whether it is our server or Clerk

7. KEEP GOOGLE BUTTON SIMPLE

* keep only disabled-once click protection
* do not add any extra blocking logic

8. AFTER FIX
   Tell me exactly:

* where the 429 was coming from
* what middleware or route caused it
* what was removed/changed
* whether sign-in now works on first click

IMPORTANT:
Do not guess.
Do not just tweak UI.
Identify the exact request returning 429 and fix that source.
This is a live production signup blocker.
